{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. On Windows decorations are off, so the custom title bar needs the window controls (macOS keeps its own, and the bar reads whether it is fullscreen); the sidebar needs the folder picker and delete confirmation.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "dialog:allow-open",
    "dialog:allow-ask"
  ]
}
